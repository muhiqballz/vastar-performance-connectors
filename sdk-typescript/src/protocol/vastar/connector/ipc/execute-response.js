"use strict";
// automatically generated by the FlatBuffers compiler, do not modify
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.ExecuteResponseT = exports.ExecuteResponse = void 0;
const flatbuffers = __importStar(require("flatbuffers"));
const error_class_1 = require("../../../vastar/connector/ipc/error-class");
/**
 * Response dari connector
 */
class ExecuteResponse {
    constructor() {
        this.bb = null;
        this.bb_pos = 0;
    }
    __init(i, bb) {
        this.bb_pos = i;
        this.bb = bb;
        return this;
    }
    static getRootAsExecuteResponse(bb, obj) {
        return (obj || new ExecuteResponse()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    static getSizePrefixedRootAsExecuteResponse(bb, obj) {
        bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
        return (obj || new ExecuteResponse()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
    }
    /**
     * Request ID yang sama dengan request
     */
    requestId() {
        const offset = this.bb.__offset(this.bb_pos, 4);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    /**
     * Success atau error
     */
    errorClass() {
        const offset = this.bb.__offset(this.bb_pos, 6);
        return offset ? this.bb.readInt8(this.bb_pos + offset) : error_class_1.ErrorClass.Success;
    }
    errorMessage(optionalEncoding) {
        const offset = this.bb.__offset(this.bb_pos, 8);
        return offset ? this.bb.__string(this.bb_pos + offset, optionalEncoding) : null;
    }
    /**
     * Response payload jika sukses
     */
    payload(index) {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.readUint8(this.bb.__vector(this.bb_pos + offset) + index) : 0;
    }
    payloadLength() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
    }
    payloadArray() {
        const offset = this.bb.__offset(this.bb_pos, 10);
        return offset ? new Uint8Array(this.bb.bytes().buffer, this.bb.bytes().byteOffset + this.bb.__vector(this.bb_pos + offset), this.bb.__vector_len(this.bb_pos + offset)) : null;
    }
    /**
     * Execution duration (microseconds)
     */
    durationUs() {
        const offset = this.bb.__offset(this.bb_pos, 12);
        return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt('0');
    }
    static startExecuteResponse(builder) {
        builder.startObject(5);
    }
    static addRequestId(builder, requestId) {
        builder.addFieldInt64(0, requestId, BigInt('0'));
    }
    static addErrorClass(builder, errorClass) {
        builder.addFieldInt8(1, errorClass, error_class_1.ErrorClass.Success);
    }
    static addErrorMessage(builder, errorMessageOffset) {
        builder.addFieldOffset(2, errorMessageOffset, 0);
    }
    static addPayload(builder, payloadOffset) {
        builder.addFieldOffset(3, payloadOffset, 0);
    }
    static createPayloadVector(builder, data) {
        builder.startVector(1, data.length, 1);
        for (let i = data.length - 1; i >= 0; i--) {
            builder.addInt8(data[i]);
        }
        return builder.endVector();
    }
    static startPayloadVector(builder, numElems) {
        builder.startVector(1, numElems, 1);
    }
    static addDurationUs(builder, durationUs) {
        builder.addFieldInt64(4, durationUs, BigInt('0'));
    }
    static endExecuteResponse(builder) {
        const offset = builder.endObject();
        return offset;
    }
    static createExecuteResponse(builder, requestId, errorClass, errorMessageOffset, payloadOffset, durationUs) {
        ExecuteResponse.startExecuteResponse(builder);
        ExecuteResponse.addRequestId(builder, requestId);
        ExecuteResponse.addErrorClass(builder, errorClass);
        ExecuteResponse.addErrorMessage(builder, errorMessageOffset);
        ExecuteResponse.addPayload(builder, payloadOffset);
        ExecuteResponse.addDurationUs(builder, durationUs);
        return ExecuteResponse.endExecuteResponse(builder);
    }
    unpack() {
        return new ExecuteResponseT(this.requestId(), this.errorClass(), this.errorMessage(), this.bb.createScalarList(this.payload.bind(this), this.payloadLength()), this.durationUs());
    }
    unpackTo(_o) {
        _o.requestId = this.requestId();
        _o.errorClass = this.errorClass();
        _o.errorMessage = this.errorMessage();
        _o.payload = this.bb.createScalarList(this.payload.bind(this), this.payloadLength());
        _o.durationUs = this.durationUs();
    }
}
exports.ExecuteResponse = ExecuteResponse;
class ExecuteResponseT {
    constructor(requestId = BigInt('0'), errorClass = error_class_1.ErrorClass.Success, errorMessage = null, payload = [], durationUs = BigInt('0')) {
        this.requestId = requestId;
        this.errorClass = errorClass;
        this.errorMessage = errorMessage;
        this.payload = payload;
        this.durationUs = durationUs;
    }
    pack(builder) {
        const errorMessage = (this.errorMessage !== null ? builder.createString(this.errorMessage) : 0);
        const payload = ExecuteResponse.createPayloadVector(builder, this.payload);
        return ExecuteResponse.createExecuteResponse(builder, this.requestId, this.errorClass, errorMessage, payload, this.durationUs);
    }
}
exports.ExecuteResponseT = ExecuteResponseT;
